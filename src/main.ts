import './style.css'

const form= document.getElementById("project-form");
const projectlist= document.getElementById("pro_list");
const projectarray=[];

form?.addEventListener("submit", async(event: SubmitEvent){
  event.preventDefault();
})